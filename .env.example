# Configuración de Ejemplo para Property Module Upgrade
# Copia este archivo a .env y ajusta los valores según tu entorno

# Base de datos
MONGO_URI=mongodb://localhost:27017
DB_NAME=propietas

# Puerto del servidor
PORT=3000

# JWT
JWT_SECRET=tu-secret-key-super-segura

# Configuración de archivos
MAX_FILE_SIZE=10485760  # 10MB en bytes
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/webp
ALLOWED_DOCUMENT_TYPES=application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document

# Rutas de almacenamiento
UPLOADS_PATH=./uploads/properties

# Configuración de imágenes (Sharp)
IMAGE_QUALITY=80
THUMB_SIZE=200
SLIDER_WIDTH=800
SLIDER_HEIGHT=600

# CORS (si usas frontend separado)
CORS_ORIGIN=http://localhost:4200,https://tu-dominio.com

# Configuración de mapa
MAP_DEFAULT_ZOOM=12
MAP_MAX_RESULTS=500

# ==========================================
# CONFIGURACIÓN DE EMAIL (SMTP) - Para envío de recibos
# ==========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-contraseña-app-password

# Email (legacy - mantener por compatibilidad)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=tu-email@gmail.com
EMAIL_PASSWORD=tu-password

# ==========================================
# CONFIGURACIÓN DE WHATSAPP BUSINESS API - Para envío de recibos
# ==========================================
# Obtener credenciales en: https://developers.facebook.com/
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_ACCESS_TOKEN=tu-access-token-aqui
WHATSAPP_PHONE_NUMBER_ID=tu-phone-number-id-aqui

# Entorno
NODE_ENV=development
